

// list of words for array 
wordsToGuess = ['carlot', 'walmart', 'house']

random selection = [house]

//number of guesses

//array for letters already guessed

guessed_letter = []

//word will have the same number of elements as random from "words to guess"

word_to_guess.length == 5

word = ['_', '_', '_', '_', '_']

//
listen for keyup

on keyup = (key) {
// loop from 0 to length of word_to_guess

for(word_to_guess.length) {
	
 }
}